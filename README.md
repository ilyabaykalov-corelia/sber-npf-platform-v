# sber-npf-platform-v

Комплект интеграции документооборота СберНПФ с Platform V: модель DataSpace, права, разрешённые GraphQL, справочники и BPMN для ПДС и КИД ОПС. Прикладные правила изменения документов принадлежат Corelia; текущие BPMN создают документы и меняют их статусы.

## Документация

- [Описание продукта](docs/product.md).
- [Описание реализации](docs/implementation.md).
- [Упаковка, проверка и установка](docs/operations.md).
- [Навигация](docs/README.md) и [инструкции для агентов](AGENTS.md).

## Сборка комплекта

Из корня репозитория, с Bash и Python 3:

```bash
bash scripts/package-platform-v.sh
```

Результат — dist/sber-npf-platform-v.zip. Состав определяется [.info.meta.json](.info.meta.json); документация и исходники соседних сервисов в архив не входят. Сборка архива не устанавливает модель и не проверяет её принятие SDK платформы.

После публикации модели до первого создания документа необходимо выполнить
[`bootstrap-document-types.graphql`](bootstrap-document-types.graphql) в конструкторе Platform V
от имени `app_owner` (или другого пользователя с правами на справочник). Выполните
операцию дважды с variables `{"id":"PDS_CONTRACT","name":"Договор ПДС"}` и
`{"id":"KID_OPS","name":"КИД ОПС"}`. Она идемпотентно публикует оба вида в
справочник `DocumentType`; без этих записей DataSpace отклоняет создание по внешнему ключу.
